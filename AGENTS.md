# Agent Instructions

Read and follow `/home/dalton/.agents/instructions/always.md`.

When this checkout is available, the canonical source is `.agents/instructions/always.md`.
Use the matching skill files under `.agents/skills/` whenever a task matches their descriptions.
