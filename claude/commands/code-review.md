---
description: Generate a GitHub code review Markdown report for an organization, owner, or the authenticated account over a time range.
---

Use the `code-review` skill with these arguments:

```text
$ARGUMENTS
```

Run `.agents/skills/code-review/scripts/github-code-review.sh $ARGUMENTS`, inspect the generated report, enrich it with a comprehensive summary and review priorities, then print the final Markdown file location.
