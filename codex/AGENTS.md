# Codex Instructions

Read and follow `/home/dalton/.agents/instructions/always.md`.

Skills are available from `~/.agents/skills`. When a task involves code edits, use `code-style` before editing. For frontend work, proactively use the relevant UI skills instead of waiting for a reminder.
